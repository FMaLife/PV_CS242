# TaskManager Class
# Responsible for managing a collection of tasks.

# Features:
# - add_task(task)
# - complete_task(task_id)
# - get_overdue_tasks()

# Encapsulation:
# - stores tasks in _tasks

# Business Logic:
# - validates task before adding
# - detects overdue tasks

# Acts as in-memory manager (not DB layer).